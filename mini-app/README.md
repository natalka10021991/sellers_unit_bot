# WB Margin Mini App 📱

React Mini App для Telegram бота расчета маржи Wildberries.

## 🚀 Быстрый старт

### 1. Установка зависимостей

Если есть проблема с npm кэшем на macOS, выполни:

```bash
# Вариант 1: Исправить права доступа (один раз)
sudo chown -R $(whoami) ~/.npm

# Вариант 2: Использовать временный кэш
npm install --cache /tmp/npm-cache

# Вариант 3: Очистить кэш
npm cache clean --force
```

Затем установи зависимости:

```bash
cd mini-app
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### 3. Тестирование Mini App

Для тестирования Mini App в Telegram:

1. **Локально с ngrok** (для тестирования):
   ```bash
   # Установи ngrok: https://ngrok.com
   ngrok http 5173
   ```
   Используй полученный HTTPS URL

2. **Или деплой на Vercel** (рекомендуется):
   ```bash
   npm install -g vercel
   vercel
   ```

## 📦 Сборка для продакшн

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🔗 Настройка в @BotFather

1. Открой @BotFather в Telegram
2. Выбери своего бота
3. Нажми "Bot Settings" → "Menu Button"
4. Установи:
   - **Text**: "📱 Калькулятор"
   - **URL**: `https://your-app.vercel.app` (твой URL после деплоя)

Или используй команду:
```
/setmenubutton
```

## 🌐 Деплой на Vercel (рекомендуется)

1. **Установи Vercel CLI**:
   ```bash
   npm install -g vercel
   ```

2. **Деплой**:
   ```bash
   cd mini-app
   vercel
   ```

3. **Получи URL** (например: `https://wb-margin-bot.vercel.app`)

4. **Обнови `.env` в корне проекта**:
   ```env
   MINI_APP_URL=https://wb-margin-bot.vercel.app
   ```

5. **Перезапусти бота** - кнопка Mini App появится автоматически!

## 🎨 Особенности

- ✨ Красивый UI с анимациями (Framer Motion)
- 📱 Адаптивный дизайн под Telegram
- 🎯 Валидация формы
- 💫 Плавные переходы
- 🌈 Градиенты и современный дизайн
- 📊 Детальные результаты расчета

## 🛠 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка
- **Tailwind CSS** - стили
- **Framer Motion** - анимации
- **Telegram WebApp API** - интеграция с Telegram

## 📝 Структура

```
mini-app/
├── src/
│   ├── App.tsx              # Главный компонент
│   ├── components/          # UI компоненты
│   │   ├── Input.tsx
│   │   ├── Button.tsx
│   │   └── ResultCard.tsx
│   └── hooks/
│       └── useTelegram.ts   # Хук для работы с Telegram API
├── index.html
└── package.json
```

## ⚠️ Важно

- Mini App **обязательно** должен быть на **HTTPS**
- Для локального тестирования используй **ngrok** или **Cloudflare Tunnel**
- После деплоя обнови `MINI_APP_URL` в `.env` бота

