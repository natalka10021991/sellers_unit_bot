# 🚀 Запуск Mini App в браузере

## Быстрый старт

### 1. Убедись, что бот запущен локально

Бот должен работать на `http://localhost:3000`:

```bash
# В корне проекта telegram-bot
cd /Users/nbystrova/projects/telegram-bot
npm run dev
```

Должны увидеть:
```
✅ .env загружен
🤖 Бот запускается...
✅ Бот @Sellers_Unit_Bot успешно запущен!
🚀 API сервер запущен на порту 3000
```

### 2. Запусти Mini App

Открой новый терминал:

```bash
cd /Users/nbystrova/projects/telegram-bot/mini-app
npm run dev
```

Должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 3. Открой в браузере

Открой в браузере:
```
http://localhost:5173
```

---

## ✅ Что должно работать

1. **Интерфейс**: Форма с полями для ввода данных
2. **Категории**: Autocomplete должен загружать категории с API бота
3. **Расчет**: Можно ввести данные и рассчитать маржу
4. **Стили**: Telegram-like дизайн

---

## 🔧 Troubleshooting

### Категории не загружаются

1. Проверь, что бот запущен на `localhost:3000`
2. Открой в браузере: `http://localhost:3000/health`
   - Должен вернуть: `{"status":"ok"}`
3. Открой в браузере: `http://localhost:3000/api/categories`
   - Должен вернуть список категорий
4. Открой консоль браузера (F12) и проверь ошибки

### CORS ошибки

Если видишь ошибки CORS:
1. Проверь, что в `src/api/server.ts` настроен CORS для `localhost:5173`
2. Перезапусти бота

### Telegram WebApp недоступен

Это нормально! В браузере Telegram WebApp SDK недоступен, но приложение работает в режиме тестирования:
- Пользователь будет показан как "Тест"
- Haptic feedback не будет работать
- Но все остальное работает нормально

---

## 📝 Примечания

- В браузере Mini App работает в "тестовом режиме"
- Для полного тестирования нужно открыть через Telegram бота
- API автоматически определяется как `http://localhost:3000` при запуске в браузере

---

## 🎯 Следующие шаги

После тестирования в браузере:
1. Задеплой бота на Railway
2. Обнови `mini-app/.env` с URL бота
3. Задеплой Mini App на Vercel
4. Протестируй через Telegram бота
