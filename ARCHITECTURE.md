# 🏗️ Архитектура проекта - простое объяснение

## 📱 Что у нас есть

Проект состоит из **двух частей**, которые работают вместе:

1. **Telegram Бот** (на Railway) - "мозги" проекта
2. **Mini App** (на Vercel) - красивый интерфейс в Telegram

---

## 🚂 Railway - где живет бот

### Что это?
**Railway** - это платформа, где работает наш Telegram бот 24/7.

### Что там происходит?
- Бот получает команды от пользователей в Telegram
- Хранит данные в базе данных (SQLite)
- Отвечает на сообщения
- Предоставляет API для Mini App (список категорий, комиссии)

### Ссылка на бота:
```
https://sellersunitbot-production.up.railway.app
```

### Как проверить, что бот работает?
Открой в браузере:
```
https://sellersunitbot-production.up.railway.app/health
```
Должен вернуться: `{"status":"ok"}`

### Как обновить бота?
1. Внеси изменения в код (в папке `src/`)
2. Закоммить изменения:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Railway **автоматически** пересоберет и перезапустит бота
4. Подожди 2-3 минуты
5. Проверь логи в Railway Dashboard → **Logs**

### Где смотреть логи бота?
1. Открой Railway Dashboard: https://railway.app
2. Выбери проект `adventurous-perfection`
3. Нажми на сервис `sellers_unit_bot`
4. Перейди во вкладку **"Logs"**

### Где настраивать переменные окружения?
1. Railway Dashboard → проект → **Settings** → **Shared Variables**
2. Добавь переменные:
   - `BOT_TOKEN` - токен бота от Telegram
   - `WB_API_TOKEN` - токен API Wildberries
   - `MINI_APP_URL` - ссылка на Mini App (Vercel)
3. **Важно:** После добавления переменной нажми **"Update Variable"** в модальном окне

---

## 🎨 Vercel - где живет Mini App

### Что это?
**Vercel** - это платформа, где размещен красивый интерфейс (Mini App), который открывается внутри Telegram.

### Что там происходит?
- Показывает форму для расчета маржи
- Автозаполнение категорий товаров
- Красивый дизайн с анимациями
- Подключается к боту на Railway для получения данных

### Ссылка на Mini App:
```
https://mini-app-red-seven.vercel.app
```

### Как обновить Mini App?
1. Внеси изменения в код (в папке `mini-app/src/`)
2. Закоммить изменения:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Vercel **автоматически** пересоберет и задеплоит Mini App
4. Подожди 1-2 минуты
5. Проверь, что все работает в Telegram боте

### Где смотреть логи Mini App?
1. Открой Vercel Dashboard: https://vercel.com
2. Выбери проект `mini-app-red-seven`
3. Перейди во вкладку **"Deployments"**
4. Нажми на последний деплой → **"View Function Logs"**

### Где настраивать переменные окружения?
1. Vercel Dashboard → проект → **Settings** → **Environment Variables**
2. Добавь переменную:
   - `VITE_API_URL` = `https://sellersunitbot-production.up.railway.app`
3. **Важно:** После добавления переменной нужно **пересобрать** Mini App:
   - Перейди в **Deployments**
   - Найди последний деплой
   - Нажми **"..."** → **"Redeploy"**

---

## 🔄 Как работают вместе

```
Пользователь в Telegram
    ↓
Нажимает кнопку "Калькулятор"
    ↓
Открывается Mini App (Vercel)
    ↓
Mini App запрашивает категории
    ↓
Запрос идет на бота (Railway)
    ↓
Бот возвращает список категорий
    ↓
Mini App показывает их пользователю
```

---

## 📝 Как вносить изменения

### Изменения в боте (Railway):

1. **Отредактируй файлы** в папке `src/`:
   - `src/bot.ts` - логика бота
   - `src/handlers/` - обработчики команд
   - `src/api/server.ts` - API для Mini App

2. **Проверь, что код компилируется:**
   ```bash
   npm run build
   ```

3. **Закоммить и запушить:**
   ```bash
   git add .
   git commit -m "Что изменил"
   git push
   ```

4. **Railway автоматически:**
   - Получит изменения из GitHub
   - Пересоберет проект
   - Перезапустит бота
   - Займет 2-3 минуты

5. **Проверь логи** в Railway Dashboard

### Изменения в Mini App (Vercel):

1. **Отредактируй файлы** в папке `mini-app/src/`:
   - `mini-app/src/App.tsx` - главный компонент
   - `mini-app/src/components/` - компоненты интерфейса

2. **Проверь локально (опционально):**
   ```bash
   cd mini-app
   npm run dev
   ```
   Откроется на `http://localhost:5173`

3. **Закоммить и запушить:**
   ```bash
   git add .
   git commit -m "Что изменил"
   git push
   ```

4. **Vercel автоматически:**
   - Получит изменения из GitHub
   - Пересоберет Mini App
   - Задеплоит новую версию
   - Займет 1-2 минуты

5. **Проверь в Telegram боте** - открой Mini App и посмотри изменения

---

## 🔗 Полезные ссылки

### Railway:
- **Dashboard:** https://railway.app
- **URL бота:** https://sellersunitbot-production.up.railway.app
- **Health check:** https://sellersunitbot-production.up.railway.app/health
- **API категорий:** https://sellersunitbot-production.up.railway.app/api/categories

### Vercel:
- **Dashboard:** https://vercel.com
- **URL Mini App:** https://mini-app-red-seven.vercel.app

### Telegram:
- **Бот:** @Sellers_Unit_Bot
- **Ссылка:** https://t.me/Sellers_Unit_Bot

---

## ⚠️ Важные моменты

### 1. Переменные окружения
- **НЕ коммить** файл `.env` в Git (он уже в `.gitignore`)
- Переменные настраиваются через Dashboard каждого сервиса
- После изменения переменных нужно перезапустить сервис

### 2. Автоматический деплой
- И Railway, и Vercel автоматически деплоят при каждом `git push`
- Не нужно ничего делать вручную после `git push`
- Просто подожди 1-3 минуты

### 3. Проверка изменений
- **Бот:** Проверь логи в Railway → **Logs**
- **Mini App:** Открой в Telegram боте и проверь визуально

### 4. Если что-то сломалось
- Проверь логи в Dashboard соответствующего сервиса
- Убедись, что переменные окружения настроены правильно
- Попробуй перезапустить сервис вручную (Redeploy)

---

## 📊 Структура проекта

```
telegram-bot/
├── src/                    # Код бота (Railway)
│   ├── bot.ts             # Главный файл бота
│   ├── api/               # API для Mini App
│   ├── handlers/          # Обработчики команд
│   └── ...
├── mini-app/              # Код Mini App (Vercel)
│   ├── src/
│   │   ├── App.tsx        # Главный компонент
│   │   └── components/    # Компоненты интерфейса
│   └── ...
├── Dockerfile             # Конфигурация для Railway
└── package.json          # Зависимости бота
```

---

## 🎯 Быстрая шпаргалка

| Что изменить | Где файлы | Куда коммитить | Где проверить |
|--------------|-----------|----------------|---------------|
| Логика бота | `src/` | `git push` → Railway | Railway Logs |
| Интерфейс Mini App | `mini-app/src/` | `git push` → Vercel | Telegram бот |
| Переменные бота | Railway Dashboard | - | Railway Logs |
| Переменные Mini App | Vercel Dashboard | Redeploy | Telegram бот |

---

## ✅ Готово!

Теперь ты знаешь:
- Где что находится
- Как вносить изменения
- Как проверить, что все работает
- Куда смотреть, если что-то сломалось

**Главное правило:** После любых изменений в коде просто делай `git push`, и все обновится автоматически! 🚀

