# 🚀 Настройка Mini App

## Быстрая установка зависимостей

Если у тебя проблема с npm кэшем на Mac, выполни **один раз**:

```bash
sudo chown -R $(whoami) ~/.npm
```

Затем:

```bash
cd mini-app
npm install
npm run dev
```

## Деплой на Vercel (5 минут)

1. **Установи Vercel CLI**:
   ```bash
   npm install -g vercel
   ```

2. **Деплой**:
   ```bash
   cd mini-app
   vercel
   ```
   - Нажми Enter на всех вопросах (по умолчанию всё правильно)
   - Получишь URL типа: `https://wb-margin-bot.vercel.app`

3. **Обнови `.env` в корне проекта**:
   ```env
   MINI_APP_URL=https://твой-url.vercel.app
   ```

4. **Перезапусти бота** - кнопка Mini App появится!

## Настройка в @BotFather

1. Открой @BotFather
2. Выбери своего бота
3. `/setmenubutton`
4. Укажи URL из Vercel

Готово! 🎉

