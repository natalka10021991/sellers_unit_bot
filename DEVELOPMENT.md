# 🛠️ Руководство по разработке

## 📋 Workflow: Dev → Prod

### Структура веток

```
main (production)     ← только проверенный код
  ↑
develop (development) ← основная ветка разработки
  ↑
feature/*            ← ветки для новых фич
```

---

## 🚀 Локальная разработка (Dev)

### 1. Настройка окружения

1. **Создай файл `.env.local`** (не коммитится в Git):
   ```bash
   cp .env.example .env.local
   ```

2. **Заполни переменные** в `.env.local`:
   ```env
   BOT_TOKEN=твой_токен_для_разработки
   WB_API_TOKEN=твой_wb_токен
   NODE_ENV=development
   API_PORT=3000
   MINI_APP_URL=http://localhost:5173
   ```

3. **Создай тестового бота** (опционально):
   - Открой @BotFather в Telegram
   - Создай нового бота для разработки
   - Используй его токен в `.env.local`

### 2. Запуск локально

```bash
# Запуск бота в режиме разработки (с автоперезагрузкой)
npm run dev

# Или явно указать окружение
npm run dev:local
```

Бот запустится на `localhost` и будет автоматически перезагружаться при изменении кода.

### 3. Запуск Mini App локально

В другом терминале:

```bash
cd mini-app
npm run dev
```

Mini App откроется на `http://localhost:5173`

### 4. Тестирование

- **Бот:** Открой тестового бота в Telegram и проверь команды
- **API:** Открой `http://localhost:3000/health` в браузере
- **Mini App:** Открой `http://localhost:5173` в браузере

---

## 🔀 Git Workflow

### Создание новой фичи

```bash
# 1. Переключись на develop
git checkout develop
git pull origin develop

# 2. Создай ветку для фичи
git checkout -b feature/название-фичи

# 3. Разрабатывай и коммить
git add .
git commit -m "feat: описание фичи"

# 4. Запушь ветку
git push origin feature/название-фичи
```

### Слияние в develop

```bash
# 1. Переключись на develop
git checkout develop
git pull origin develop

# 2. Слей feature ветку
git merge feature/название-фичи

# 3. Запушь
git push origin develop
```

### Деплой на production

```bash
# 1. Убедись, что все протестировано в develop
# 2. Переключись на main
git checkout main
git pull origin main

# 3. Слей develop в main
git merge develop

# 4. Запушь в main
git push origin main

# 5. Railway и Vercel автоматически задеплоят
```

---

## ✅ Чеклист перед коммитом

Перед каждым коммитом проверь:

- [ ] Код компилируется: `npm run build`
- [ ] Нет ошибок типов: `npm run typecheck`
- [ ] Нет `console.log` (используй `logger`)
- [ ] Нет секретов в коде (токены, пароли)
- [ ] `.env.local` не коммитится (уже в `.gitignore`)
- [ ] Код протестирован локально

---

## 🧪 Тестирование перед деплоем

### Перед коммитом в develop:

1. **Локальное тестирование:**
   ```bash
   npm run dev
   # Проверь все функции в тестовом боте
   ```

2. **Проверка типов:**
   ```bash
   npm run typecheck
   ```

3. **Сборка:**
   ```bash
   npm run build
   ```

### Перед слиянием в main (production):

1. **Все проверки из develop**
2. **Тестирование на Railway staging** (если есть)
3. **Проверка переменных окружения** в Railway
4. **Проверка логов** после деплоя

---

## 🔧 Настройка окружений

### Development (локально)

- **NODE_ENV:** `development`
- **База данных:** `./data/bot.db` (локальная)
- **API Port:** `3000`
- **Mini App:** `http://localhost:5173`
- **Логирование:** Подробное, в консоль

### Production (Railway/Vercel)

- **NODE_ENV:** `production`
- **База данных:** На сервере Railway
- **API Port:** `3000` (внутренний)
- **Mini App:** `https://mini-app-red-seven.vercel.app`
- **Логирование:** Структурированное, в Railway Logs

---

## 📝 Правила коммитов

Используй [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавлена новая функция
fix: исправлена ошибка
docs: обновлена документация
refactor: рефакторинг кода
test: добавлены тесты
chore: обновлены зависимости
```

Примеры:
- `feat: добавлен автокомплит категорий`
- `fix: исправлен ввод в поле категории`
- `docs: обновлен ARCHITECTURE.md`
- `refactor: заменены console.log на logger`

---

## 🚨 Что НЕ делать

❌ **НЕ коммить:**
- `.env.local` файлы
- Токены и секреты
- `node_modules/`
- `dist/` (собранные файлы)
- Временные файлы

❌ **НЕ пушить напрямую в `main`:**
- Всегда через `develop`
- Или через Pull Request

❌ **НЕ деплоить без тестирования:**
- Всегда тестируй локально
- Проверяй на develop перед main

---

## 🔍 Отладка

### Локально

```bash
# Запуск с подробными логами
NODE_ENV=development DEBUG=* npm run dev

# Проверка переменных окружения
node -e "console.log(process.env)"
```

### На Railway

1. Открой Railway Dashboard → **Logs**
2. Фильтруй по уровню (ERROR, WARN, INFO)
3. Проверь переменные окружения в **Settings** → **Variables**

---

## 📚 Полезные команды

```bash
# Разработка
npm run dev              # Запуск с автоперезагрузкой
npm run dev:local        # Явно указать development

# Проверка
npm run typecheck        # Проверка типов TypeScript
npm run lint             # Проверка кода
npm run build            # Сборка проекта

# Git
git status               # Статус изменений
git diff                 # Просмотр изменений
git log --oneline        # История коммитов

# Очистка
rm -rf dist/             # Удалить собранные файлы
rm -rf node_modules/     # Удалить зависимости
npm ci                   # Чистая установка зависимостей
```

---

## ✅ Готово!

Теперь у тебя есть:
- ✅ Разделение dev/prod окружений
- ✅ Git workflow с ветками
- ✅ Локальная разработка и тестирование
- ✅ Безопасный деплой на production

**Помни:** Всегда тестируй локально перед деплоем на production! 🚀


