# 🚀 Деплой на продакшен

## Вариант 1: Railway (рекомендуется)

### 1. Подготовка

1. Зарегистрируйся на [Railway.app](https://railway.app)
2. Подключи GitHub репозиторий

### 2. Настройка проекта

1. Создай новый проект из GitHub репозитория
2. Railway автоматически определит Node.js проект

### 3. Переменные окружения

Добавь в настройках проекта (Settings → Variables):

```env
BOT_TOKEN=твой_токен_от_BotFather
WB_API_TOKEN=твой_токен_от_WB
MINI_APP_URL=https://mini-app-red-seven.vercel.app
NODE_ENV=production
API_PORT=3000
```

### 4. Команды запуска

Railway автоматически определит:
- **Build Command**: `npm run build`
- **Start Command**: `npm start`

### 5. Деплой

Railway автоматически задеплоит при каждом push в main ветку.

---

## Вариант 2: Render

### 1. Подготовка

1. Зарегистрируйся на [Render.com](https://render.com)
2. Подключи GitHub репозиторий

### 2. Создание Web Service

1. New → Web Service
2. Подключи репозиторий
3. Настройки:
   - **Name**: `wb-margin-bot`
   - **Environment**: `Node`
   - **Build Command**: `npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free или Starter

### 3. Переменные окружения

В разделе Environment Variables добавь:

```env
BOT_TOKEN=твой_токен_от_BotFather
WB_API_TOKEN=твой_токен_от_WB
MINI_APP_URL=https://mini-app-red-seven.vercel.app
NODE_ENV=production
API_PORT=3000
```

### 4. Деплой

Render автоматически задеплоит при каждом push.

---

## Вариант 3: VPS (через PM2)

### 1. Подключение к серверу

```bash
ssh user@your-server.com
```

### 2. Установка зависимостей

```bash
# Установи Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установи PM2
sudo npm install -g pm2
```

### 3. Клонирование репозитория

```bash
git clone https://github.com/your-username/telegram-bot.git
cd telegram-bot
```

### 4. Настройка окружения

```bash
# Создай .env файл
nano .env
```

Добавь:
```env
BOT_TOKEN=твой_токен_от_BotFather
WB_API_TOKEN=твой_токен_от_WB
MINI_APP_URL=https://mini-app-red-seven.vercel.app
NODE_ENV=production
API_PORT=3000
```

### 5. Установка и сборка

```bash
npm install
npm run build
```

### 6. Запуск с PM2

```bash
pm2 start dist/index.js --name "wb-margin-bot"
pm2 save
pm2 startup
```

### 7. Проверка

```bash
pm2 status
pm2 logs wb-margin-bot
```

---

## Проверка работы

После деплоя проверь:

1. **Бот отвечает на команды**:
   - Открой бота в Telegram
   - Отправь `/start`
   - Должно прийти приветствие

2. **API сервер работает**:
   - Открой `https://твой-домен.com/health`
   - Должен вернуться `{"status":"ok"}`

3. **Mini App открывается**:
   - Нажми кнопку "📱 Калькулятор" в боте
   - Должно открыться Mini App

---

## Обновление

### Railway / Render

Просто сделай push в main ветку - автоматический деплой.

### VPS

```bash
cd telegram-bot
git pull
npm install
npm run build
pm2 restart wb-margin-bot
```

---

## Мониторинг

### Railway
- Логи доступны в Dashboard
- Метрики в разделе Metrics

### Render
- Логи в разделе Logs
- Метрики в разделе Metrics

### VPS (PM2)
```bash
pm2 logs wb-margin-bot    # Просмотр логов
pm2 monit                 # Мониторинг в реальном времени
pm2 status                # Статус процессов
```

---

## Troubleshooting

### Бот не отвечает

1. Проверь переменные окружения
2. Проверь логи: `pm2 logs` или в Dashboard
3. Убедись, что `BOT_TOKEN` правильный

### API не работает

1. Проверь, что порт `API_PORT` открыт
2. Проверь CORS настройки
3. Проверь логи API сервера

### Mini App не открывается

1. Проверь `MINI_APP_URL` в переменных окружения
2. Убедись, что Mini App задеплоен на Vercel
3. Проверь настройки в @BotFather

---

## Готово! 🎉

Твой бот теперь работает на продакшене!


