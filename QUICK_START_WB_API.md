# 🚀 Быстрый старт: WB API интеграция

## ✅ Что сделано:

1. ✅ Создан сервис для работы с WB API (`src/services/wb-api.ts`)
2. ✅ Создан HTTP сервер-прокси в боте (`src/api/server.ts`)
3. ✅ Добавлен endpoint `/api/categories` для получения категорий
4. ✅ Создан компонент Autocomplete в Mini App
5. ✅ Интегрирован autocomplete в форму расчета

## 📝 Что нужно сделать:

### 1. Добавить WB API токен в `.env`

Открой файл `.env` в корне проекта и добавь:

```env
WB_API_TOKEN=твой_токен_от_wb
```

**Где взять токен:**
1. Зайди на https://dev.wildberries.ru/
2. Войди в личный кабинет
3. Создай токен для категории **"Контент"**
4. Скопируй токен в `.env`

### 2. Установить зависимости

```bash
npm install
```

### 3. Запустить бота

```bash
npm run dev
```

Должно появиться:
```
✅ .env загружен
🌐 API сервер запущен на http://localhost:3000
🤖 Бот запускается...
✅ Бот @Sellers_Unit_Bot успешно запущен!
```

### 4. Проверить работу API

Открой в браузере: `http://localhost:3000/api/categories`

Должен вернуться JSON со списком категорий.

### 5. Обновить Mini App

В Mini App нужно указать URL API бота. 

**Для локальной разработки:**
В файле `mini-app/src/App.tsx` уже есть:
```typescript
const API_URL = import.meta.env.VITE_API_URL || "http://localhost:3000";
```

**Для продакшена:**
После деплоя бота на сервер, создай файл `mini-app/.env`:
```env
VITE_API_URL=https://your-bot-api-url.com
```

## 🎯 Как это работает:

1. Пользователь открывает Mini App
2. В форме появляется поле "Категория товара" с autocomplete
3. При вводе показываются категории из WB API
4. При выборе категории автоматически заполняется комиссия WB
5. Пользователь вводит остальные данные и рассчитывает маржу

## 📡 Доступные API endpoints:

- `GET /health` - проверка работоспособности
- `GET /api/categories` - список родительских категорий WB
- `GET /api/categories/:id/subjects` - предметы (подкатегории)
- `GET /api/commission/:categoryId` - комиссия для категории

## ⚠️ Важно:

- API токен хранится на сервере (безопасно)
- Категории кэшируются на 1 час
- Лимит WB API: 100 запросов в минуту

## 🔧 Troubleshooting:

**Ошибка "WB API токен не установлен":**
- Проверь `.env` файл
- Убедись, что токен начинается правильно

**Ошибка 401:**
- Проверь правильность токена
- Убедись, что токен для категории "Контент"

**Категории не загружаются:**
- Проверь, что API сервер запущен
- Проверь консоль браузера на ошибки CORS
- В продакшене нужен HTTPS

